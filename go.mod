// gapline — highlights time gaps in any log stream: instantly see where
// the four seconds went.
//
// version:    0.1.0
// author:     JaydenCJ
// license:    MIT
// repository: https://github.com/JaydenCJ/gapline
// keywords:   logs, latency, timestamps, gap-detection, pipe-filter, perf-debugging, developer-tools
//
// Zero runtime dependencies: the require list below is intentionally empty
// and must stay that way (see CONTRIBUTING.md, "Ground rules").
module github.com/JaydenCJ/gapline

go 1.22
